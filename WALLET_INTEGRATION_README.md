# Kanari Bank - Wallet Integration Guide

การผสาน `kanari-crypto` เข้ากับระบบโอนเงินใน `kanari` เรียบร้อยแล้ว! ตอนนี้คุณสามารถใช้ระบบกระเป๋าเงินที่มีความปลอดภัยสูงพร้อมลายเซ็นดิจิทัล

## 🔑 ฟีเจอร์ใหม่

### 1. สร้างกระเป๋าเงินใหม่ (CreateWallet)

สร้างกระเป๋าเงินพร้อม mnemonic phrase และเข้ารหัสด้วย password

```bash
cargo run --bin kanari -- create-wallet --password "your-secure-password" --curve ed25519
```

**Curve Types ที่รองรับ:**

- `ed25519` - Ed25519 (เร็ว, ปลอดภัย) ⭐ แนะนำ
- `k256` - Secp256k1 (Bitcoin/Ethereum compatible)
- `p256` - Secp256r1 (NIST P-256)
- `dilithium2` - Post-Quantum Cryptography Level 2 🔮
- `dilithium3` - Post-Quantum Cryptography Level 3 🔮
- `dilithium5` - Post-Quantum Cryptography Level 5 🔮

**ผลลัพธ์:**

```
🔑 Generated Mnemonic (SAVE THIS SECURELY!):
   abandon ability able about above absent absorb abstract absurd abuse access accident

✓ Wallet created successfully!
  Address: 0x1234567890abcdef...
  Curve: Ed25519
  Account registered in Move VM
```

### 2. โหลดกระเป๋าเงิน (LoadWallet)

เปิดกระเป๋าเงินที่มีอยู่แล้วด้วย address และ password

```bash
cargo run --bin kanari -- load-wallet --address "0x1234..." --password "your-password"
```

**ผลลัพธ์:**

```
✓ Wallet loaded successfully!
  Address: 0x1234567890abcdef...
  Curve: Ed25519
  Balance: 1000 coins
```

### 3. ดูรายการกระเป๋าเงินทั้งหมด (ListWallets)

แสดงกระเป๋าเงินทั้งหมดพร้อมยอดเงิน

```bash
cargo run --bin kanari -- list-wallets
```

**ผลลัพธ์:**

```
Available Wallets:
Address                                                          Balance
==================================================================================
0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef            1000
0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890             500
```

### 4. โอนเงินแบบมีลายเซ็น (SignedTransfer)

โอนเงินพร้อมยืนยันตัวตนด้วยลายเซ็นดิจิทัล

```bash
cargo run --bin kanari -- signed-transfer \
  --from "0x1234..." \
  --to "0xabcd..." \
  --amount 100 \
  --password "your-password"
```

**ผลลัพธ์:**

```
✓ Transaction signed
  Signature: 1a2b3c4d5e6f...

✓ Signed transfer completed
  From: 0x1234... (balance: 900)
  To: 0xabcd... (balance: 600)
  Amount: 100
```

## 📋 ขั้นตอนการใช้งานแบบเต็ม

### Step 1: สร้างกระเป๋าเงิน 2 อัน

```bash
# กระเป๋าที่ 1 (Alice)
cargo run --bin kanari -- create-wallet --password "alice123" --curve ed25519

# กระเป๋าที่ 2 (Bob)
cargo run --bin kanari -- create-wallet --password "bob456" --curve ed25519
```

**บันทึก address และ mnemonic phrase ของทั้งสองกระเป๋า!**

### Step 2: เติมเงินเข้ากระเป๋า

```bash
cargo run --bin kanari -- mint --amount 1000 --recipient "0x<alice-address>"
```

### Step 3: โอนเงินแบบมีลายเซ็น

```bash
cargo run --bin kanari -- signed-transfer \
  --from "0x<alice-address>" \
  --to "0x<bob-address>" \
  --amount 100 \
  --password "alice123"
```

### Step 4: ตรวจสอบยอดเงิน

```bash
# ดูยอดเงินทั้งหมด
cargo run --bin kanari -- list-wallets

# หรือดูกระเป๋าเฉพาะ
cargo run --bin kanari -- balance --address "0x<alice-address>"
```

## 🔒 ความปลอดภัย

### การเข้ารหัส

- กระเป๋าเงินถูกเข้ารหัสด้วย AES-256-GCM
- ข้อมูลถูกบีบอัดก่อนเข้ารหัสเพื่อลดขนาด
- Password ต้องมีความยาวอย่างน้อย 8 ตัวอักษร

### Mnemonic Phrase

- ใช้มาตรฐาน BIP-39 (12 words)
- **สำคัญมาก:** เก็บ mnemonic phrase ไว้ในที่ปลอดภัย!
- สามารถใช้กู้คืนกระเป๋าเงินได้หากลืม password

### ลายเซ็นดิจิทัล

- ทุก transaction ในโหมด `signed-transfer` จะถูกลงนามด้วย private key
- ป้องกันการปลอมแปลงและยืนยันตัวตนผู้ส่ง
- รองรับ Post-Quantum Cryptography (Dilithium)

## 🔮 Post-Quantum Cryptography

Kanari Bank รองรับอัลกอริทึม Post-Quantum สำหรับป้องกันการโจมตีจาก Quantum Computer ในอนาคต:

```bash
# สร้างกระเป๋าแบบ Quantum-Safe
cargo run --bin kanari-bank -- create-wallet \
  --password "secure-password" \
  --curve dilithium3
```

**ระดับความปลอดภัย:**

- `dilithium2` - NIST Level 2 (เร็วกว่า)
- `dilithium3` - NIST Level 3 (แนะนำ)
- `dilithium5` - NIST Level 5 (ปลอดภัยสูงสุด)

## 💾 ที่เก็บข้อมูล

### Keystore

กระเป๋าเงินถูกเก็บใน:

```
~/.kanari/keystore/keystore.yaml
```

### State

ข้อมูล account และ transaction ถูกเก็บใน:

```
./data/move_state.json
```

## ⚠️ ข้อควรระวัง

1. **อย่าแชร์ password และ mnemonic phrase**
2. **อย่าลืม password** - ไม่สามารถกู้คืนได้ถ้าลืม
3. **สำรองข้อมูล keystore** เป็นประจำ
4. **ใช้ password ที่รัดกุม** อย่างน้อย 16 ตัวอักษร แนะนำ

## 🔧 คำสั่งเดิมที่ยังใช้ได้

คุณยังสามารถใช้คำสั่งแบบเดิมได้ตามปกติ:

```bash
# สร้าง account แบบเดิม (ไม่มี wallet)
cargo run --bin kanari -- create-account --address "0x1234..."

# โอนเงินแบบเดิม (ไม่ต้องใส่ password)
cargo run --bin kanari -- transfer --from "0x1234..." --to "0xabcd..." --amount 100

# Mint เงิน
cargo run --bin kanari -- mint --amount 1000 --recipient "0x1234..."
```

## 🚀 สิ่งที่พัฒนาเพิ่มเติมได้

- [ ] กู้คืนกระเป๋าจาก mnemonic phrase
- [ ] HD Wallet (BIP-44) สำหรับสร้างหลาย address จาก seed เดียว
- [ ] Multi-signature transactions
- [ ] Hardware wallet support (HSM)
- [ ] Web UI สำหรับจัดการกระเป๋า

## 📚 เอกสารเพิ่มเติม

- [POST_QUANTUM_GUIDE.md](crates/kanari-crypto/POST_QUANTUM_GUIDE.md) - คู่มือ Post-Quantum Cryptography
- [SECURITY_ENHANCEMENTS.md](crates/kanari-crypto/SECURITY_ENHANCEMENTS.md) - ฟีเจอร์ความปลอดภัย
- [MOVE_VM_USAGE.md](crates/kanari/MOVE_VM_USAGE.md) - การใช้งาน Move VM

---

**สร้างโดย:** Kanari Team  
**อัพเดตล่าสุด:** 24 พฤศจิกายน 2025
